.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "TG_BRIDGE" "1" "February 2026" "tg_bridge 0.3"
.SH NAME
tg_bridge \- read\-only Telegram channel bridge CLI for AI agents
.SH SYNOPSIS
\f[B]tg_bridge\f[R] [\f[B]\-V\f[R]|\f[B]\(enversion\f[R]]
\f[I]command\f[R] [\f[I]options\f[R]]
.PP
\f[B]tg_bridge list\f[R]
.PP
\f[B]tg_bridge history\f[R] {\f[B]\(enchannel\f[R]
\f[I]name\f[R]|\f[B]\(enchannel_id\f[R] \f[I]id\f[R]}
\f[B]\(enafter\f[R] \f[I]date\f[R] [\f[B]\(enlimit\f[R] \f[I]n\f[R]]
.PP
\f[B]tg_bridge sync\f[R] {\f[B]\(enchannel\f[R]
\f[I]name\f[R]|\f[B]\(enchannel_id\f[R] \f[I]id\f[R]}
[\f[B]\(enlimit\f[R] \f[I]n\f[R]]
.PP
\f[B]tg_bridge download\f[R] {\f[B]\(enchannel\f[R]
\f[I]name\f[R]|\f[B]\(enchannel_id\f[R] \f[I]id\f[R]}
\f[B]\(enmessage_id\f[R] \f[I]id\f[R] \f[B]\(enoutput\f[R] \f[I]dir\f[R]
.SH DESCRIPTION
\f[B]tg_bridge\f[R] is a read\-only Telegram channel bridge designed for
AI agents to consume Telegram channel messages.
It connects to Telegram using the MTProto protocol via the Telethon
library and outputs all data as JSON for machine parsing.
.PP
The tool supports incremental synchronization with persistent state
tracking, allowing AI agents to efficiently poll for new messages
without re\-fetching historical data.
.PP
All operations are read\-only; the tool never sends messages or modifies
channel content.
.SH COMMANDS
.TP
\f[B]list\f[R]
List all Telegram channels accessible to the authenticated user.
Returns an array of channel objects with \f[B]id\f[R], \f[B]name\f[R],
and \f[B]username\f[R] fields.
.TP
\f[B]history\f[R]
Fetch messages from a channel starting after a specific date/time.
Useful for one\-time historical queries.
Does not update sync state.
.TP
\f[B]sync\f[R]
Incremental synchronization that retrieves only new messages since the
last sync.
Automatically tracks and persists the last seen message ID per channel.
This is the recommended command for regular polling.
.TP
\f[B]download\f[R]
Download a media attachment from a specific message.
Supports photos, documents, videos, and audio files.
.SH OPTIONS
.SS Global Options
.TP
\f[B]\-V\f[R], \f[B]\(enversion\f[R]
Display version information and exit.
.SS Channel Selection (required for history, sync, download)
.TP
\f[B]\(enchannel\f[R] \f[I]name\f[R]
Channel username (without \(at) or display name.
.TP
\f[B]\(enchannel_id\f[R] \f[I]id\f[R]
Numeric channel ID.
Can be specified with or without the leading minus sign (both
\f[B]\-1001234567890\f[R] and \f[B]1001234567890\f[R] are accepted).
.SS Command\-Specific Options
.TP
\f[B]\(enafter\f[R] \f[I]date\f[R]
(history only, required) ISO 8601 formatted datetime to fetch messages
after.
Format: \f[B]YYYY\-MM\-DDTHH:MM:SS\f[R]
.TP
\f[B]\(enlimit\f[R] \f[I]n\f[R]
(history, sync) Maximum number of messages to retrieve.
Default: \f[B]50\f[R]
.TP
\f[B]\(enmessage_id\f[R] \f[I]id\f[R]
(download only, required) The numeric ID of the message containing the
media to download.
.TP
\f[B]\(enoutput\f[R] \f[I]dir\f[R]
(download only, required) Directory path where the downloaded file will
be saved.
Created automatically if it does not exist.
.SH OUTPUT FORMAT
All output is JSON.
Message objects contain:
.IP \(bu 2
\f[B]id\f[R] \- Numeric message ID
.IP \(bu 2
\f[B]date\f[R] \- ISO 8601 timestamp
.IP \(bu 2
\f[B]sender\f[R] \- Sender\(cqs numeric user ID
.IP \(bu 2
\f[B]text\f[R] \- Message text content (may be null for media\-only
messages)
.IP \(bu 2
\f[B]media\f[R] \- (optional) Media metadata object with \f[B]type\f[R],
\f[B]file_name\f[R], \f[B]mime_type\f[R], and \f[B]size\f[R]
.PP
Media types: \f[B]photo\f[R], \f[B]document\f[R], \f[B]video\f[R],
\f[B]audio\f[R]
.PP
Download command returns:
.IP \(bu 2
\f[B]message_id\f[R] \- The requested message ID
.IP \(bu 2
\f[B]channel_id\f[R] \- The channel\(cqs numeric ID
.IP \(bu 2
\f[B]file_path\f[R] \- Absolute path to the downloaded file
.IP \(bu 2
\f[B]media_type\f[R] \- Type of media downloaded
.IP \(bu 2
\f[B]size\f[R] \- File size in bytes
.PP
Errors are returned as: \f[B]{\(lqerror\(rq: \(lqdescription\(rq}\f[R]
.SH FILES
.TP
\f[B]\(ti/.config/tg_bridge/\f[R]
Configuration directory (created automatically with mode 0700)
.TP
\f[B]\(ti/.config/tg_bridge/session.session\f[R]
Telegram authentication session file.
Created on first run after interactive authentication.
.TP
\f[B]\(ti/.config/tg_bridge/channel_state.json\f[R]
Persistent sync state storing the last seen message ID for each channel.
Used by the \f[B]sync\f[R] command.
.SH EXAMPLES
List all accessible channels:
.IP
.EX
tg_bridge list
.EE
.PP
Get messages from a channel after a specific date:
.IP
.EX
tg_bridge history \-\-channel_id \-1001234567890 \-\-after 2026\-01\-15T00:00:00
.EE
.PP
Sync new messages (incremental):
.IP
.EX
tg_bridge sync \-\-channel MyChannel \-\-limit 100
.EE
.PP
Download media from message 42:
.IP
.EX
tg_bridge download \-\-channel_id 1001234567890 \-\-message_id 42 \-\-output ./downloads
.EE
.SH FIRST RUN
On first execution, \f[B]tg_bridge\f[R] will prompt for interactive
authentication:
.IP "1." 3
Enter your phone number (international format)
.IP "2." 3
Enter the verification code sent to your Telegram app
.IP "3." 3
If enabled, enter your two\-factor authentication password
.PP
The session is then persisted and subsequent runs require no
interaction.
.SH EXIT STATUS
.TP
\f[B]0\f[R]
Success
.TP
\f[B]non\-zero\f[R]
Error (details in JSON output)
.SH ENVIRONMENT
The tool does not use environment variables.
API credentials are compiled into the binary.
.SH SEE ALSO
\f[B]jq\f[R](1) for parsing JSON output
.PP
Project repository: https://github.com/abrandt/tg_bridge
.SH BUGS
Report bugs at: https://github.com/abrandt/tg_bridge/issues
.SH AUTHORS
Andy Brandt.
